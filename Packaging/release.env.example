# Copy this file to something like Packaging/release.env and source it before packaging.

export BUNDLE_ID="com.loqbar.app"
export MARKETING_VERSION="0.1.0"
export BUILD_NUMBER="1"

# Manual in-app update checks can read either:
# - a GitHub latest-release endpoint, e.g. https://api.github.com/repos/OWNER/REPO/releases/latest
# - or a custom JSON manifest URL that returns { "version": "...", "download_url": "..." }
export RELEASE_FEED_URL="https://api.github.com/repos/GePlusE/LoqBar/releases/latest"
export RELEASE_PAGE_URL="https://github.com/GePlusE/LoqBar/releases"

# Real distribution signing identity:
# export SIGNING_IDENTITY="Developer ID Application: Your Name (TEAMID)"

# A notarytool keychain profile created with:
# xcrun notarytool store-credentials "LoqBarNotary" --apple-id ... --team-id ... --password ...
# export KEYCHAIN_PROFILE="LoqBarNotary"
